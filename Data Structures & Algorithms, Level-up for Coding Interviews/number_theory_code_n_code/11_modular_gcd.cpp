#include "bits/stdc++.h"
using namespace std;

int main(){
    // Given integers A, B and N, you should calculate the GCD of A^N + B^N and
    // A-B (Assume that GCD(0, a) = a for any positive integer a) Since this
    // number coukd be very large, compute t modulo 1000000007 (10 +7)   
    return 0;
}